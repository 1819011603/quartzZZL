

1. 删除文件
	``` shell
	fd pic.jpg | runeach 'echo \"`pwd`/var\"' | runeach rm
\	```


##### 输出当前文件夹下的所有文件的绝对路径

这个命令会在当前目录下执行 `find` 命令，查找并输出所有类型为文件的项目的绝对路径。

xargs -I {} 分别执行命令

find "$(pwd)" -type f | xargs -I {} python /root/ossUpload.py {}



