# 2026-09-16 续班关系配置 · 判单命中续班关系但课程无续班前置配置

## 检索信息

- 状态：排查中
- 关键词：续班关系,续班前置课程,course_relation_map,listContinuationCourseInfoByNumberList,listByBizNumbers,判单,预报名膨胀券
- 关键 ID：问题订单428744800036979907,前置订单427870435939451587,23KXTJ26QK8K1,22KXTJ26QLZE1

## 反馈信息

- 反馈渠道：飞书群转发，王永诗提出
- 涉及对象：prod 环境。问题订单 428744800036979907（课程 23KXTJ26QK8K1），前置订单 427870435939451587（课程 22KXTJ26QLZE1）。王永诗在判单过程列表（`https://sd.baijia.com/projectItem/myreview/96644b0f-e9ac-4cce-b092-b7cdb242fb9a/3ceb19cd-f7cb-4b92-9d2e-17c1a1d14c1a`）看到判单原因显示命中了续班关系，但在 OES「续班设置」页搜 22KXTJ26QLZE1 是否为 23KXTJ26QK8K1 的续班前置课程，搜到 0/0，怀疑是自己查法有问题

## 结论

不是王永诗查法的问题：数据侧确认 22KXTJ26QLZE1 和 23KXTJ26QK8K1 之间在课程中心 `course_relation_map` 里当前**从未配置过**任何方向的续班关系（含历史软删记录）。OES 页面「共27条」与直接调用续班关系接口的结果一致，都不包含对方课程。判单结果里"命中续班关系"这句结论的依据不是这两门课在 `course_relation_map` 里的直接配置，具体命中的是哪张表/哪个字段，还需要看判单服务自身的明细（本次未能从 myreview 页面拿到，见"当前状态与处置"）。

## 关键证据

- 课程号转换（`CourseQueryApiFeignClient#listByBizNumbers`，prod）：22KXTJ26QLZE1 → 数字课程号 576342575829551104；23KXTJ26QK8K1 → 数字课程号 547895220000272384
- 续班关系接口（`CourseNotNormalQueryApiFeignClient#listContinuationCourseInfoByNumberList`，入参 numbers=[547895220000272384]，prod）返回 27 条，全部 `calculateRenewalType=1`（普通续班），条数与 OES「续班设置」页「详细数据 共27条」一致；27 条 `relationNumber` 中不含 576342575829551104
- 已排除的错误查法：`CourseNotNormalAclServiceImpl#mapPreCourseByNumber` 只保留 `calculateRenewalType==2`（预报名类型），对本案例这种普通续班关系（type=1）会直接返回空，不能用它验证是否存在续班关系
- 直接查库 `course_center.course_relation_map`（prod cluster_id=317）：精确匹配 `(course_number=547895220000272384, relation_number=576342575829551104)` 及反向，均 0 条，且不限 `isdel`（覆盖历史软删）
- `trace_tree`（traceId `e536de14-29f2-47a4-90d4-ea9bbaf2fcaf.0.1`）确认 `listContinuationCourseInfoByNumberList` 下游直接落到 course-center 的 `CourseRelationMapMapper#selectByExample`，查的就是 `course_center.course_relation_map`，中间没有缓存或旁路数据源

## 当前状态与处置

数据侧已排除 `course_relation_map` 表本身存在该配置的可能，结论待判单服务/风控侧确认"命中续班关系"具体引用的字段或数据源（例如是否命中的课程号搞反、走的是班级维度而非课程维度、或判单时点用的是其它系统的续班关系判断）。下一步需要有权限打开 myreview 判单明细页面的同事（或判单服务的负责人）核实具体命中依据；本次未能通过 `baijia-proxy` 直接抓到该前端页面渲染出的判单明细（页面是 SPA，接口鉴权/数据需要登录态渲染，直接 curl 只能拿到空壳 HTML）。

## 涉及系统

- 仓库 / 服务：student-data（ACL 封装 `CourseQueryAdapter`、`CourseNotNormalQueryAdapter`）、course-center（维护并提供 `course_relation_map` 及其 Feign 接口）
- 代码 / 数据：`course_center.course_relation_map`（prod cluster_id=317，test cluster_id=153，`mysql_locate_cluster` 可查）；`student-data-client` 的 `com.gaotu.student.data.client.acl.adapter.CourseQueryAdapter#listByBizNumbers`；`student-data-service` 的 `com.gaotu.student.data.infrastructure.acl.impl.CourseNotNormalAclServiceImpl` 及其适配器 `CourseNotNormalQueryAdapter#listContinuationCourseInfoByNumberList`
- 排查 runbook：[`../排查/判单命中续班关系但配置查不到.md`](../排查/判单命中续班关系但配置查不到.md)
