# 退费预测分层配置 · 某班学生集中标为高危

> **只解决**：某班/某批学生"AI预测退费意向"高危占比异常。
> 两次事故根因都是 `predict_intent_config` 缺精细配置后退化到 `defaultGrade` 兜底，
> **先查配置再怀疑代码或算法**。

> **只解决**：某班/某批学生高危退费占比异常。两次事故根因都是 `predict_intent_config`
> 缺精细配置后退化到 `defaultGrade` 兜底，**先查配置再怀疑代码或算法**。


适用：收到「某班/某批学生高危退费占比异常」类反馈。**先查配置缺口，再怀疑代码或算法模型**
——两次真实事故都不是代码 bug，而是 `ees_data.predict_intent_config` 缺精细配置后静默退化到
`defaultGrade` 兜底阈值。

## 0. 先分清两个 job（最容易搞混）

| | `refundPredictionCheckHandler` | `SyncRefundResultHandler` |
|---|---|---|
| 作用 | **每天自动产出**「AI预测退费意向」 | **仅手动补数/回溯历史** |
| prod job | job_id=10073，jobGroup=314（`高途-运营工作台-student-data`） | job_id=12513「退费分层回溯」、10251「退费结果回溯」，jobGroup=530（`student-data-dws`） |
| 状态 | triggerStatus=1 运行中，cron `0 0 6,9 * * ?` | triggerStatus=0 **已停止，不会自动跑** |
| 入口方法 | `RenewalComponent#refreshRefundSnapshotIntentRecord` | `RenewalComponent#refreshRefundResultIntentRecord` |

**推论：阈值配置改完不需要人工干预**，次日例行调度会自动用新配置处理当天新产出的记录。
只有要修**历史存量**记录时才需要下面第 2 步的回溯。查 job 现状走 `xjob-admin` skill：
`xjob_list_jobs -k handler=<refundPredictionCheckHandler 或 SyncRefundResultHandler>`。

## 1. 定位是不是配置缺口

配置表 `ees_data.predict_intent_config`，结果表 `ees_data.ai_refund_prediction_results`，均 PROD
`cluster_id=336`（走 `mysql-query` skill）。维度是「业务线 × 学部 × 学期 × stage × 年级 × 新老生」，
任一组合缺精确配置即退化到 `grade='defaultGrade'` 兜底行；阈值区间存在 `intent_config` 一个 JSON
字段里（`[{code,start,end}]` 四档），不是拆开的独立列。具体 SQL 见 [`Scripts/排查.sql`](../Scripts/排查.sql)。

判据：**某个 `prediction_level`（尤其 4=高）占比畸高**，且精确配置查不到、对应组合
`grade_specific_rows=0` → 基本可判定为配置缺口。

⚠️ **补完配置要顺手验一件事**：2026-09-14 发现新补的配置里有阈值区间宽度为 0（`intent_config`
四档里中间两档 `start==end` 重合，比如精品班学部/初三/老生那条）的情况，疑似算法侧样本量不足
导致分位点重合。补完配置后核对一遍（`排查.sql` 第5步），命中重合就先别回溯，反馈算法侧确认。

## 2. 决定要不要回溯

- 只影响**当天及以后** → 不用回溯，等次日例行调度。
- 要修**历史存量** → 走 [`Scripts/回溯.sh`](../Scripts/回溯.sh)。
- **配置只补了一部分时不要做全量回溯**：回溯只能修好已补齐的那部分，剩下的仍吃兜底，跑完反而
  让人误以为已经全好了。等配完整、且阈值区间无异常再统一回溯。

## 3. 回溯历史数据

命令见 [`Scripts/回溯.sh`](../Scripts/回溯.sh)。走 `student-data-dws` 的 prod pod，用 `pod-terminal`
skill 的 arthas HTTP API（优先于 `baijia-invoke` 的 HTTP 桥——那条路容易碰到"未找到服务实现类"或
登录失效）。**必须显式传完整时间区间**：默认空参数只回溯"今天"，会静默漏掉全部历史存量记录且跑完
不报错，"跑完没报错"不能作为历史数据已更新的验证信号（2026-09-11 案例用的是 `20260815`~`20260908`）。

## 4. 验证

1. DB：重跑「排查.sql」第1步的分层分布，确认高危占比回落。
2. ES：查 `ads_large_subclazz_user_index`，确认与 DB 一致（走 `es-apply` skill，**PROD ES 不能用
   es-aliyun MCP**）。
3. 两边不一致说明同步链路有问题（`DwsRefundIntentConsumer`），不是阈值问题。

2026-09-11 参考值：补配置+回溯后，高一数学班从 110 人 92 高危 → 97 人 8 高危，DB 与 ES 一致。

配置读取有 **300 秒本地缓存**，改完配置后立即验证会读到旧值，等 5 分钟。

依赖的全局 skill：`xjob-admin`（查/触发 job）、`mysql-query`（查配置与结果）、`pod-terminal`
（arthas 回溯）、`es-apply`（查 PROD ES）。

不属于本案例范围：`feature-predict-level-reason` 重构分支是另一套逻辑；两次事故都发生在
**重构前的旧逻辑**上。

## 历史案例

> 只记录当时可确认的现象、证据、结论和处置，**不要拿来当当前口径**；
> 需要复用的方法要提到上面正文里。新案例按日期倒序插在本节最前。

### 2026-09-11 H 业务线秋季班多班级集中标为高危退费

#### 检索信息

- 状态：待处理
- 最后核验：2026-09-14
- 关键词：AI预测退费意向，高危退费，批量误判，predict_intent_config，defaultGrade，stage=2，stage=3，stage=4，高一，高三，秋季班，refundPredictionCheckHandler，退费预测结果同步，SyncRefundResultHandler，退费分层回溯，退费结果回溯
- 关键 ID：班级 `33DLTS26QX6P11001`、`31SXTS26QK4X11005`，clazz_number `545524684567900160`、`551189826087448576`，subclazz_number `35957125251465472`，xjob `refundPredictionCheckHandler`(prod job_id=10073, jobGroup=314)、`SyncRefundResultHandler`(prod job_id=12513/10251, jobGroup=530)

#### 反馈信息

- 反馈渠道：飞书群聊转发
- 涉及对象：【2026秋季】高考地理目标决胜班、【2026秋季】高一数学目标领航班

#### 当前结论

不是代码 bug。`ees_data.predict_intent_config` 对 H 业务线秋季班的部分 stage/年级组合缺少精细阈值时，会退化到 `defaultGrade` 粗粒度兜底；不适配的兜底阈值会导致学生集中进入高危档。

#### 关键证据

| 班级 | 配置范围与现象 | 当前结果 |
|---|---|---|
| 高考地理目标决胜班 `33DLTS26QX6P11001` | `stage=4`、高三；1789 人中 1599 人为高危，分组记录 1607 条且几乎全部 `level=4`；H 业务线四个学部没有对应年级精细阈值，使用 `score > 0.16` 的兜底档 | 待处理，见下方 2026-09-14 复核 |
| 高一数学目标领航班 `31SXTS26QK4X11005` | `stage=2/3`、高一；补配置前 110 人中 92 人为高危 | 已解决；补齐阈值并回溯后为 97 人中 8 人高危，DB 与 ES 一致 |

**2026-09-14 复核 H 业务线/秋季班/stage=4 四个学部的配置补齐进度**（`predict_intent_config` 现状，均为
2026-09-14 15:50~15:53 新写入）：

| 学部 | 已补组合 | 仍缺 |
|---|---|---|
| 精品班学部（两次案例都在这个学部） | 7 个（高二/高三 新老生 + 初三 新老生） | 高一·老生 |
| 本地化大班学部 | 1 个（初三·新生） | 高一/高二/高三 全部年级、各年级老生 |
| 菁英班学部 | 2 个（初三 新老生） | 高一/高二/高三 全部 |
| 清北班学部 | 0 个 | 全部，仍只有 defaultGrade 兜底，未动过 |

新补的配置里发现两条阈值区间疑似有问题（中间档区间宽度为 0，`start==end`）：精品班学部/初三/老生
`[0~0.2544697, 0.2544697~0.2544697, 0.2544697~0.2544697, 0.2544697~1]`；菁英班学部/初三/新生同样
在 0.8397 处重合。疑似算法侧样本量不足导致分位点重合，**已作为待确认项反馈算法侧，尚未回复**。

#### 当前状态与处置

- **两个 job 不要混淆**：
  - `refundPredictionCheckHandler`（"退费预测结果同步"，prod job_id=10073，jobGroup=314
    `高途-运营工作台-student-data`，cron `0 0 6,9 * * ?` 每天6点/9点各跑一次，**triggerStatus=1
    运行中**）才是**每天自动产出**"AI预测退费意向"的 job，内部调
    `RenewalComponent#refreshRefundSnapshotIntentRecord` 读 `predict_intent_config`（300秒本地缓存）。
    **阈值配置改了之后不需要人工干预**，下一次例行调度会自动用新配置处理当天新产出的记录。
  - `SyncRefundResultHandler` 只是**手动补数**用的 job，prod 下两条记录
    （job_id=12513"退费分层回溯"、job_id=10251"退费结果回溯"，均 jobGroup=530
    `student-data-dws`）**triggerStatus 都是 0（已停止）**，参数还停留在过期的
    `{"startTime":"20251030","endTime":"20251031"}`，**不会自动跑**，只用于人工触发回溯历史存量数据
    （2026-09-08 案例即通过 arthas 反射调用此 handler 完成，等效于手动触发这个已停止的 job；具体命令见 [`Scripts/回溯.sh`](../Scripts/回溯.sh) 与 [`某班学生集中标为高危.md`](某班学生集中标为高危.md)）。
- `stage=4`：仍不能做全量回溯——精品班学部之外的三个学部大部分年级组合仍在吃兜底，且新补的两条配置
  阈值区间本身可能有问题。**建议等配置真正配完整、且确认阈值区间无异常后再统一回溯**，现在回溯只能
  修好精品班学部一小部分。
- `stage=2/3`：阈值已补齐，并通过 `SyncRefundResultHandler#execute` 完成约 56 万条记录回溯与结果验证，
  已解决。
- 回溯必须显式传入目标完整时间区间；该案例使用 `20260815` 至 `20260908`。默认参数只覆盖当天，不能作为
  历史数据已全部更新的验证信号。

#### 涉及系统

- 仓库 / 服务：`student-data`、`student-data-service`、`student-data-dws`、`student-data-facade`
- 代码 / 数据：`RenewalComponent#refreshRefundResultIntentRecord`（手动回溯路径）、
  `RenewalComponent#refreshRefundSnapshotIntentRecord`（每日例行路径，`RefundPredictionService` 调用）、
  `PredictIntentConfigServiceImpl#listByInfo`/`#listDefaultByInfo`、`SyncRefundResultHandler`、
  `RefundPredictionCheckHandler`、`DwsRefundIntentConsumer`；`ees_data.predict_intent_config`、
  `ees_data.ai_refund_prediction_results`（cluster_id `336`，PROD）、ES 索引 `ads_large_subclazz_user_index`
- 排查 runbook：[`某班学生集中标为高危.md`](某班学生集中标为高危.md) + `Scripts/`（可执行脚本）；依赖全局 `xjob-admin`、`mysql-query`、`pod-terminal`、`es-apply` skill
