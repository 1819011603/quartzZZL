# 2026-09-11 退费预测分层配置 · H业务线秋季班多班级集中标为高危退费

## 检索信息

- 状态：待处理
- 最后核验：2026-09-15
- 关键词：AI预测退费意向，高危退费，批量误判，predict_intent_config，defaultGrade，stage=2，stage=3，stage=4，高一，高三，秋季班，refundPredictionCheckHandler，退费预测结果同步，SyncRefundResultHandler，退费分层回溯，退费结果回溯，清北班学部
- 关键 ID：班级 `33DLTS26QX6P11001`、`31SXTS26QK4X11005`，clazz_number `545524684567900160`、`551189826087448576`，subclazz_number `35957125251465472`，xjob `refundPredictionCheckHandler`(prod job_id=10073, jobGroup=314)、`SyncRefundResultHandler`(prod job_id=12513/10251, jobGroup=530)，回溯执行记录 logId=4681326757

## 反馈信息

- 反馈渠道：飞书群聊转发
- 涉及对象：【2026秋季】高考地理目标决胜班、【2026秋季】高一数学目标领航班

## 结论

不是代码 bug。`ees_data.predict_intent_config` 对 H 业务线秋季班的部分 stage/年级组合缺少精细阈值时，会退化到 `defaultGrade` 粗粒度兜底；不适配的兜底阈值会导致学生集中进入高危档。

## 关键证据

| 班级 | 配置范围与现象 | 当前结果 |
|---|---|---|
| 高考地理目标决胜班 `33DLTS26QX6P11001` | `stage=4`、高三；1789 人中 1599 人为高危，分组记录 1607 条且几乎全部 `level=4`；H 业务线四个学部没有对应年级精细阈值，使用 `score > 0.16` 的兜底档 | 精品班学部（该班所在学部）已解决，见下方 2026-09-15 半月回溯复核；清北班学部仍待处理 |
| 高一数学目标领航班 `31SXTS26QK4X11005` | `stage=2/3`、高一；补配置前 110 人中 92 人为高危 | 已解决；补齐阈值并回溯后为 97 人中 8 人高危，DB 与 ES 一致 |

**2026-09-14 复核 H 业务线/秋季班/stage=4 四个学部的配置补齐进度**（`predict_intent_config` 现状，均为
2026-09-14 15:50~15:53 新写入）：

| 学部 | 已补组合 | 仍缺 |
|---|---|---|
| 精品班学部（两次案例都在这个学部） | 7 个（高二/高三 新老生 + 初三 新老生） | 高一·老生 |
| 本地化大班学部 | 1 个（初三·新生） | 高一/高二/高三 全部年级、各年级老生 |
| 菁英班学部 | 2 个（初三 新老生） | 高一/高二/高三 全部 |
| 清北班学部 | 0 个 | 全部，仍只有 defaultGrade 兜底，未动过 |

新补的配置里发现两条阈值区间疑似有问题（中间档区间宽度为 0，`start==end`）：精品班学部/初三/老生
`[0~0.2544697, 0.2544697~0.2544697, 0.2544697~0.2544697, 0.2544697~1]`；菁英班学部/初三/新生同样
在 0.8397 处重合。疑似算法侧样本量不足导致分位点重合，**已作为待确认项反馈算法侧，尚未回复**。

**2026-09-15 半月回溯 + 复核**：触发 `SyncRefundResultHandler`(job_id=12513) 按时间范围
`20260830`~`20260914` 全系统回溯（未按班级/学部过滤，这个 job 本来就不支持），`xjob_job_logs`
确认 `handleCode=200` 成功（`logId=4681326757`；执行器机器事后下线，业务日志正文已取不到，
但 handleCode 存在 xjob-admin 中心库不受影响）。回溯后复核 H 业务线/秋季班/stage=4 四学部的
`ai_refund_prediction_results.prediction_level` 分布：

| 学部 | 高危(4) | 低危(1) | 状态 |
|---|---|---|---|
| 本地化大班学部 | 52 | 6227 | ✅ 已恢复正常 |
| 精品班学部 | 817 | 292361 | ✅ 已恢复正常 |
| 菁英班学部 | 60 | 8755 | ✅ 已恢复正常 |
| 清北班学部 | 482 | 37 | ❌ 仍畸高(~70%) |

清北班学部不是回溯失败——复查 `predict_intent_config`，这个学部的 stage=4 从 2026-09-08 至今
**一直没有任何年级细分配置**，只有当初那条 `defaultGrade` 兜底（`update_time` 仍是 09-08 19:40:54，
未变过）。回溯只能修已配置的部分，这个学部配置压根没补，回溯了也没用。

## 当前状态与处置

- **两个 job 不要混淆**：
  - `refundPredictionCheckHandler`（"退费预测结果同步"，prod job_id=10073，jobGroup=314
    `高途-运营工作台-student-data`，cron `0 0 6,9 * * ?` 每天6点/9点各跑一次，**triggerStatus=1
    运行中**）才是**每天自动产出**"AI预测退费意向"的 job，内部调
    `RenewalComponent#refreshRefundSnapshotIntentRecord` 读 `predict_intent_config`（300秒本地缓存）。
    **阈值配置改了之后不需要人工干预**，下一次例行调度会自动用新配置处理当天新产出的记录。
  - `SyncRefundResultHandler` 只是**手动补数**用的 job，prod 下两条记录
    （job_id=12513"退费分层回溯"、job_id=10251"退费结果回溯"，均 jobGroup=530
    `student-data-dws`）**triggerStatus 都是 0（已停止）**，参数还停留在过期的
    `{"startTime":"20251030","endTime":"20251031"}`，**不会自动跑**，只用于人工触发回溯历史存量数据
    （具体命令见 [`../Scripts/回溯.sh`](../Scripts/回溯.sh) 与 [`../排查/某班学生集中标为高危.md`](../排查/某班学生集中标为高危.md)）。
- `stage=4`：2026-09-15 已做半月回溯（`20260830`~`20260914`），本地化大班学部/精品班学部/菁英班学部
  三个学部已恢复正常。**清北班学部仍待处理**——配置从未补过，需要继续推动算法侧补齐该学部 stage=4
  的年级细分阈值，补齐后针对该学部再回溯一次（不用等全系统范围，配置补完当天例行调度
  `refundPredictionCheckHandler` 也会自动处理当天新产出的记录，只有该学部**历史存量**需要手动回溯）。
- `stage=2/3`：阈值已补齐，并通过 `SyncRefundResultHandler#execute` 完成约 56 万条记录回溯与结果验证，
  已解决。
- 回溯必须显式传入目标完整时间区间；历史用过 `20260815`~`20260908`、`20260830`~`20260914` 两个区间。
  默认参数只覆盖当天，不能作为历史数据已全部更新的验证信号。

## 涉及系统

- 仓库 / 服务：`student-data`、`student-data-service`、`student-data-dws`、`student-data-facade`
- 代码 / 数据：`RenewalComponent#refreshRefundResultIntentRecord`（手动回溯路径）、
  `RenewalComponent#refreshRefundSnapshotIntentRecord`（每日例行路径，`RefundPredictionService` 调用）、
  `PredictIntentConfigServiceImpl#listByInfo`/`#listDefaultByInfo`、`SyncRefundResultHandler`、
  `RefundPredictionCheckHandler`、`DwsRefundIntentConsumer`；`ees_data.predict_intent_config`、
  `ees_data.ai_refund_prediction_results`（cluster_id `336`，PROD）、ES 索引 `ads_large_subclazz_user_index`
- 排查 runbook：[`../排查/某班学生集中标为高危.md`](../排查/某班学生集中标为高危.md) + `Scripts/`（可执行脚本）；依赖全局 `xjob-admin`、`mysql-query`、`pod-terminal`、`es-apply` skill
