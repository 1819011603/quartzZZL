# 工单案例索引

这里保存高途线上问题的当前案例快照，与 `需求/` 分开。每个业务模块只有一个目录和一份 `README.md`；同一根因更新原案例，只有独立根因才新增日期小节。

## 使用规则

- 收到“查工单、查问题、帮我看看线上反馈”时，先使用全局 `gaotu-ticket-triage` skill 搜索本目录。
- 按原始报错、字段名、接口名、userId、班级号、traceId 等高区分度信息检索；命中记录只作线索，当前对象、现象和证据链一致后才能复用结论。
- 只有用户明确同意保存或更新时才写入。写前先检查同模块、同现象、关键 ID 和根因，避免重复案例。
- 每条记录只保留当前可确认的现象、证据、结论和处置状态；被推翻的假设、失败尝试、完整聊天和失效下一步直接删除。
- 通用 SQL、日志语法、接口重放和回溯步骤维护在对应 skill/reference；本目录只保留案例证据和有效指针。
- 新独立案例按 `gaotu-ticket-triage/references/record-format.md` 写入对应模块 README。写完同步本索引的记录数、当前状态和高区分度结论。

## 索引

- [企微侧边栏](企微侧边栏/README.md) - 1 条，非缺陷：亲密称呼为空时前端使用姓名兜底，后端字段 `10001` 与 `60008` 没有联动。
- [退费预测分层配置](退费预测分层配置/README.md) - 1 条，待处理（最后核验 2026-09-11）：H 业务线秋季班部分 stage/年级缺少精细阈值，退化到 `defaultGrade` 后集中标为高危。
