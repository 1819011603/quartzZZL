---
title: 退费预测分层配置 · 会话日志
tags: [需求, 日志]
---

# 会话日志

> **只追加，倒序（新的在上）。任何时候不许改写或删除旧条目。**
> 两个执行者在同一条时间轴上：`🤖 Claude` 和 `👤 我`。

---

## 2026-09-11（案例2）

### 🤖 Claude
- 排查班级 33DLTS26QX6P11001（1789人中1599人"高危"），定位到 `clazz_number=545524684567900160`
  下挂了很多个 `subclazz_number`，按 clazz 维度聚合发现问题集中在
  H业务线/精品班学部/秋季班/**stage=4**/高三（新生+老生共1607条几乎全是level=4）
- 推翻案例1时的假设"缺配置只是零散的单个组合"：查 `predict_intent_config` 发现
  **stage=4 这个阶段，H业务线下全部4个学部（本地化大班/清北班/精品班/菁英班）在"秋季班"
  这一学期都只有 `defaultGrade` 粗粒度兜底、没有任何年级细分**，是系统性缺口，
  不是单个班/单个组合的问题 —— 因为兜底阈值本身门槛过低（score>0.16 即判高危）套用到
  高三学生身上普遍偏高的分数区间，导致大批误判
- 结论落 [[README]]「已定共识」+「下一步」，待办落 T-01（[[tasks]]）；因为算法侧配置尚未补上，
  本次**没有执行回溯**（配置没配对，回溯没有意义）

### 留给下次
- 待算法/数据侧补齐 stage=4 配置后，限定 stage=4 触发一次回溯，别再扫全量（案例1教训）

---

## 2026-09-08（案例1）

### 🤖 Claude
- 排查班级 31SXTS26QK4X11005（付乐乐带班，110人中92人"高危"），定位根因：
  H业务线/精品班学部/秋季班/stage=3/高一 组合缺细分阈值配置，落到粗粒度兜底档
- 确认线上现跑的是重构前旧逻辑（`feature-predict-level-reason` 分支未合并到 release/master）
- 配置补齐后，触发 `SyncRefundResultHandler` 回溯：第一次用默认参数（空）只回溯了"今天"，
  抽查发现该班仍有4条历史记录（updated_time 非今天）未被覆盖 → 推翻"默认参数够用"的假设，
  改用显式 `{"startTime":"20260815","endTime":"20260908"}` 补跑一次，覆盖了这批历史存量
- 预估回溯总耗时时，错误套用了另一次运行日志里"每页100条"的批次大小去推算总页数，
  导致预估17小时；实际该次运行批次大小是1000条/页，实际约2小时跑完 —— **推翻**："别用
  跨运行的批次大小去估算，要用当次日志实际反推"（已写入 [[README]] 坑位）
- 验证：数据库 `prediction_level` + ES 索引 `ads_large_subclazz_user_index` 的
  `aiRefundIntentScoreResult` 双侧核实，该班"高危"人数从92降到8，与阈值配置修复后的预期一致

### 留给下次
- 关注 xjob-admin skill 上线后，以后触发 `SyncRefundResultHandler` 优先走该 skill 的
  XXL-Job 管理台，比 arthas 反射调用更规范 → 已完成（见本目录建立时的 runbook，2026-09-12）
