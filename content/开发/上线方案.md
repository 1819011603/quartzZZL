

## 索引？？



# sql


```
ALTER TABLE tb_sales_cas_wxid_u_record ADD COLUMN robot_type TINYINT(3) DEFAULT 0 COMMENT '0为PC机器人,1为移动端';
```

```
ALTER TABLE tb_sales_friend_reply_by_channel_conf ADD COLUMN robot_type int(11) NOT NULL DEFAULT '0' COMMENT '机器人类型，0：PC机器人，1：移动机器人';
```


```
CREATE TABLE `tb_sales_chatroom_reply_plan` (
  `id` int(11) unsigned NOT NULL AUTO_INCREMENT COMMENT '主键ID',
  `cas_id` varchar(128) NOT NULL COMMENT 'casId, 邮箱前缀',
  `plan_id` bigint(20) NOT NULL COMMENT '方案ID',
  `plan_name` varchar(255) NOT NULL COMMENT '方案名',
  `plan_status` tinyint(3) NOT NULL DEFAULT '0' COMMENT '是否开启 1开启 0关闭',
  `send_type` int(11) NOT NULL DEFAULT '0' COMMENT '发送方式，1：群内任意号发送，2：指定号顺序发送',
  `order_logic_ids` varchar(1024) NOT NULL DEFAULT '' COMMENT '指定号顺序发送时，机器人的logicId列表',
  `time_interval` int(11) DEFAULT NULL COMMENT '时间间隔，单位s',
  `max_member` int(11) DEFAULT NULL COMMENT '最大人数',
  `create_time` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `update_time` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  PRIMARY KEY (`id`) USING BTREE,
  UNIQUE KEY `uk_plan_id` (`plan_id`) USING BTREE,
  KEY `idx_cas_id` (`cas_id`) USING BTREE
) ENGINE=InnoDB AUTO_INCREMENT=29 DEFAULT CHARSET=utf8mb4 COMMENT='进群回复配置方案';
```


`   alter` `table` `tb_sales_chatroom_reply_task` `add` `column` `` `plan_id` `` `bigint``(20)` `NOT` `NULL` `DEFAULT` `'0'` `COMMENT` `'方案ID'``;`

`alter` `table` `tb_sales_chatroom_reply_task` `add` `index` `` `idx_plan_id` (`plan_id`); ``


`alter` `table` `tb_sales_keyword_reply_plan`

`add` `column` `` `cas_id` `` `varchar``(128)` `NOT` `NULL` `DEFAULT` `''` `COMMENT` `'casId, 邮箱前缀'``,`

`add` `column` `` `trigger_type` tinyint(3) `` `NOT` `NULL` `DEFAULT` `2 COMMENT` `'触发类型：0 关键词  1 图片  2 同时支持'``;`

`alter` `table` `tb_sales_keyword_reply_plan` `add` `index` `` `idx_cas_id` (`cas_id`) USING BTREE; ``


`CREATE` `TABLE` `` `tb_sales_keyword_reply_target` ( ``

  `` `id` `` `int``(11)` `NOT` `NULL` `AUTO_INCREMENT,`

  `` `plan_id` `` `bigint``(64)` `NOT` `NULL` `COMMENT` `'方案ID'``,`

  `` `target` `` `varchar``(64)` `NOT` `NULL` `COMMENT` `'生效对象ID'``,`

  `` `target_type` tinyint(3) `` `NOT` `NULL` `COMMENT` `'生效对象是号或者群 0 号 1群'``,`

  `` `ext` `` `varchar``(64)` `NOT` `NULL` `COMMENT` `'生效对象的补充信息'``,`

  `` `create_time` `` `timestamp` `NOT` `NULL` `DEFAULT` `CURRENT_TIMESTAMP` `COMMENT` `'创建时间'``,`

  `` `update_time` `` `timestamp` `NOT` `NULL` `DEFAULT` `CURRENT_TIMESTAMP` `ON` `UPDATE` `CURRENT_TIMESTAMP` `COMMENT` `'更新时间'``,`

  `PRIMARY` `KEY` ``(`id`) USING BTREE,``

  `KEY` `` `idx_target` (`target`, `target_type`) USING BTREE ``

`) ENGINE=InnoDB` `DEFAULT` `CHARSET=utf8mb4 COMMENT=``'关键字回复生效对象'``;`